interface:
  display_name: "Code Review"
  short_description: "Review Spec Kit changes by repository layer"
  default_prompt: "Use $code-review to review this branch against its Spec Kit artifacts and layered repository constraints."
