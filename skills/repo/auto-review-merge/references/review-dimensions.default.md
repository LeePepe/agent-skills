1. 明显 bug / 崩溃 / 数据破坏 / 并发错误 / 资源泄漏 / 未处理的错误路径。
2. 安全:硬编码密钥、注入、未校验的外部输入、CI/workflow 的提权或可被 PR 篡改的信任边界。
3. 架构/依赖:违反本项目的分层或模块边界(如下层依赖上层、跨界 import)。
4. 改了源码却完全没有对应测试改动(除非 commit message 显式说明豁免原因)。
5. 公开 API / 行为的破坏性变更而无迁移说明。
